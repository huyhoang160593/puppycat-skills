# Skill naming: `teaching` (forked from `teach`)

The teach skill was forked to add subject subdirectories and HTML formatting. The fork needed a distinct name to avoid collision with upstream `teach` in `.agents/`. Chose `teaching` over alternatives (`learning`, `teach-gen`, `curriculum`) because it's process-focused, clearly distinct from `teach`, and matches the stateful multi-session philosophy.
